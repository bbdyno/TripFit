{
  "name": "tripfit-firebase-rules-tests",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:rules": "node --test test/*.test.mjs",
    "test:emulator": "firebase emulators:exec --only firestore,auth --project demo-tripfit --config ../firebase.json 'npm run test:rules'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
